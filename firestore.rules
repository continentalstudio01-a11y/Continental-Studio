rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read and write for the biosite configuration and customer interactions (orders/leads)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
